{
  "name": "aiseoshift",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node ./scripts/og-svg-to-png.mjs",
    "preview": "npm run build && wrangler dev",
    "astro": "astro",
    "images:fetch": "node ./scripts/fetch-editorial-images.mjs",
    "generate-types": "wrangler types",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.5.1",
    "@astrojs/mdx": "^5.0.0",
    "@astrojs/rss": "^4.0.17",
    "@astrojs/sitemap": "^3.7.1",
    "astro": "^6.0.4",
    "html2canvas-pro": "^2.3.8",
    "jspdf": "^4.2.1",
    "rehype-external-links": "^3.0.0",
    "sharp": "^0.34.3"
  },
  "devDependencies": {
    "playwright": "^1.60.0",
    "wrangler": "^4.92.0"
  }
}
