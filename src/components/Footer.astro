---
const today = new Date();
---

<footer class="site-footer">
	<div class="footer-wrap">
		<p class="footer-mark">AISEOShift</p>
		<p class="footer-copy">
			Reporting on how search, AI answers, and authority systems are changing in real time.
		</p>
		<div class="footer-links">
			<a href="/blog">Latest coverage</a>
			<a href="/about">About the publication</a>
		</div>
		<p class="footer-meta">&copy; {today.getFullYear()} AISEOShift. All rights reserved.</p>
	</div>
</footer>

<style>
	.site-footer {
		padding: 3rem 0 4rem;
		border-top: 1px solid rgba(var(--ink), 0.08);
		background:
			linear-gradient(180deg, rgba(var(--paper), 0), rgba(var(--ink), 0.04)),
			rgb(var(--paper));
	}

	.footer-wrap {
		width: min(1200px, calc(100% - 2rem));
		margin: 0 auto;
		display: grid;
		grid-template-columns: minmax(0, 1.2fr) minmax(0, 0.8fr);
		gap: 2rem;
	}

	.footer-mark {
		margin: 0;
		font-family: 'Georgia', 'Times New Roman', serif;
		font-size: 1.75rem;
	}

	.footer-copy,
	.footer-meta {
		max-width: 42rem;
		color: rgba(var(--ink), 0.72);
	}

	.footer-meta {
		font-size: 0.85rem;
		margin-top: 0.5rem;
	}

	.footer-links {
		display: flex;
		gap: 1.25rem;
		margin: 1.5rem 0;
		flex-wrap: wrap;
	}

	.footer-links a {
		text-decoration: none;
		color: rgb(var(--ink));
		font-weight: 700;
		font-size: 0.9rem;
	}

	.footer-links a:hover,
	.footer-links a:focus-visible {
		color: var(--signal);
	}

	@media (max-width: 800px) {
		.site-footer {
			padding: 2rem 0 3rem;
		}

		.footer-wrap {
			grid-template-columns: 1fr;
			gap: 1.25rem;
		}

		.footer-mark {
			font-size: 1.5rem;
		}

		.footer-links {
			margin: 1rem 0;
		}
	}
</style>
